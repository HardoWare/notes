export default defineAppConfig({
	ui: {
		colors: {
			primary: 'rose',
			neutral: 'neutral',
		},
		button: { defaultVariants: { variant: 'ghost' } },
		select: {
			variants: {
			
			}
		}
	},
})
